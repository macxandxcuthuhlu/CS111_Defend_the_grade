import math

def factorial(num):
    count = 1
    for i in range(int(num)):
        i += 1
        count = count * i
    return count

counter = 4
runner = 0
while runner < math.pow(10000000, 4):
    counter += 4
    runner = (factorial(counter)/factorial(counter-4)+2)
print(counter)
print(runner)